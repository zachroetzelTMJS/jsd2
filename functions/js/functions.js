console.log("template loaded");
console.log("functions")

function multiply(x, y) {
	var product = x * y;
	return product;
}

function subtract(x, y) {
	var difference = x - y;
	return difference;
}

function divide(x, y) {
	var quotient = x / y;
	return quotient;
}

function add(x, y) {
	var sum = x + y;
	return sum;
}